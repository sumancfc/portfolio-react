import React from "react";

const title = (props) => (
  <div className='row'>
    <div className='section__big-title'>
      <h2 className='section__title'>{props.title}</h2>
    </div>
  </div>
);

export default title;
